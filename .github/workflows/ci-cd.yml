name: CI/CD Pipeline

on:
  push:
    branches: [python-sample]
  pull_request:
    branches: [python-sample]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lint with flake8
        run: |
          pip install flake8
          flake8 app

      - name: Run tests
        run: |
          pip install pytest
          pytest tests

  docker:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/python-sample'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ vars.DOCKER_USERNAME }}/docker-python-sample:latest

  # deploy:
  #   needs: docker
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Deploy to server (example with SSH)
  #       uses: appleboy/ssh-action@v1.2.0
  #       with:
  #         host: ${{ secrets.HOST }}
  #         username: ${{ secrets.USERNAME }}
  #         key: ${{ secrets.SSH_KEY }}
  #         script: |
  #           docker pull -b python-sample ${{ vars.DOCKER_USERNAME }}/docker-python-sample:latest
  #           docker stop python-app || true
  #           docker rm python-app || true
  #           docker run -d --name python-app -p 80:80 ${{ vars.DOCKER_USERNAME }}/docker-python-sample:latest
